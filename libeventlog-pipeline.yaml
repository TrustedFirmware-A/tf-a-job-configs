- job:
    name: libeventlog-pipeline
    description: |
      Job triggered for every Event Log Library (LibEventLog) patch, verifying code
      style, and building the module.
    project-type: pipeline
    sandbox: true
    dsl: !include-raw-verbatim: libeventlog-pipeline/Jenkinsfile
