- job:
    name: "tf-a-libtpm-pipeline"

    description: |
      Job triggered for every TPM Library (LibTPM) patch, verifying code
      style, and building the module.

    project-type: "pipeline"
    sandbox: true
    dsl: !include-raw-verbatim: "tf-a-libtpm-pipeline/Jenkinsfile"
