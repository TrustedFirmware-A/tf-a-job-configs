- job:
    name: "tf-a-lts2.10-gerrit-tforg-l1"
    node: "docker-amd64-tf-a-lts2.10-jammy"
    project-type: "multijob"
    concurrent: true
    disabled: false

    description: |
      Job that triggers for every TF-A patch approved with the Allow-CI+1
      label on review.trustedfirmware.org.

    properties:
      - "tf-a-clone-properties"

      - tf-a-ci-scripts:
          download_server_url: "https://downloads.trustedfirmware.org"
          download_server_tf_a_url: "https://downloads.trustedfirmware.org/tf-a-lts-v2.10"

      - build-discarder:
          days-to-keep: 30

    parameters:
      - tf-a-gerrit-comment-added-parameters:
          gerrit_project: "{gerrit_project_prefix}TF-A/trusted-firmware-a"
          gerrit_branch: "lts-v2.10"
          gerrit_refspec: "lts-v2.10"

      - tf-a-builder-parameters:
          tf_a_project: "${{GERRIT_PROJECT}}"
          tf_a_branch: "${{GERRIT_REFSPEC}}"

          tftf_project: "{gerrit_project_prefix}TF-A/tf-a-tests"
          tftf_branch: "lts-v2.10"

          tf_a_ci_scripts_mbed_tls_version: "3.6.4"

    wrappers:
      - credentials-binding:
          - ssh-user-private-key:
              credential-id: "TFA_CI_BOT_USER_SSH_KEY"
              key-file-variable: "CI_BOT_KEY"
              username-variable: "CI_BOT_USERNAME"
              passphrase-variable: ""

      - "timestamps"

    builders:
      - multijob:
          name: "Lint commit messages"
          condition: "COMPLETED"

          projects:
            - name: "tf-a-commitlint"
              kill-phase-on: "NEVER"

              predefined-parameters: |
                URL=https://review.trustedfirmware.org/${{GERRIT_PROJECT}}
                REFSPEC=${{GERRIT_REFSPEC}}
                REFNAME=${{GERRIT_PATCHSET_REVISION}}
                REFNAME_BASE=origin/${{GERRIT_BRANCH}}

      - multijob:
          condition: "COMPLETED"
          name: "Build and static checks"

          projects:
            - kill-phase-on: "NEVER"
              name: "tf-a-static-checks"
              current-parameters: true

              predefined-parameters: |-
                TEST_GROUPS=tf-l2-scan-build-lts-v2.10

      - multijob:
          condition: "COMPLETED"
          name: "Run doc build check"

          projects:
            - kill-phase-on: "NEVER"
              name: "tf-a-lts2.10-ci-gateway"
              current-parameters: true

              predefined-parameters: |
                TEST_GROUPS=tf-l1-check-docs-lts-v2.10

      - multijob:
          condition: "COMPLETED"
          name: "Run build tests"

          projects:
            - kill-phase-on: "NEVER"
              name: "tf-a-lts2.10-ci-gateway"
              current-parameters: true

              predefined-parameters: |
                TEST_GROUPS=tf-l1-build-fvp-lts-v2.10 tf-l1-build-juno-lts-v2.10 tf-l1-build-clang-lts-v2.10 tf-l1-build-plat-lts-v2.10 spm-l1-build-tests-lts-v2.10

    triggers:
      - gerrit:
          server-name: "review.trustedfirmware.org"

          trigger-on:
            - comment-added-event:
                approval-category: "Allow-CI"
                approval-value: 1

          projects:
            - project-compare-type: "PLAIN"
              project-pattern: "{gerrit_project_prefix}TF-A/trusted-firmware-a"

              branches:
                - branch-compare-type: "PLAIN"
                  branch-pattern: "lts-v2.10"

          override-votes: true
          gerrit-build-started-verified-value: 0
          gerrit-build-successful-verified-value: 1
          gerrit-build-failed-verified-value: -1
          gerrit-build-unstable-verified-value: -1
          gerrit-build-notbuilt-verified-value: 0
          gerrit-build-started-codereview-value: 0
          gerrit-build-successful-codereview-value: 0
          gerrit-build-failed-codereview-value: 0
          gerrit-build-unstable-codereview-value: 0
          gerrit-build-notbuilt-codereview-value: 0
