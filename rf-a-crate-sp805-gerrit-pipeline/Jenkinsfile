cratePipeline(
    name: 'arm-sp805',
    project: 'arm-firmware-crates',

    gerrit: [
        host: params.GERRIT_HOST,
        project: params.GERRIT_PROJECT,
        refspec: params.GERRIT_REFSPEC,
        branch: params.GERRIT_BRANCH,
    ],
)
